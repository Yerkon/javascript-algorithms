

describe('test task', () => {
    it('should check', () => {

        let k = 3 + 2;

        expect(1).toEqual(1);
    });

    // it('should test', () => {
    //     let trie = new TrieNode('a');

    //     expect(trie).toBeDefined();
    // });
});